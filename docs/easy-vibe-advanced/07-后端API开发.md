# 第 7 章：后端 API 开发

---

## 本章目标

- 理解后端与 API 的概念
- 掌握 API Route 的使用
- 理解 RESTful 规范
- 掌握参数的三种来源
- 理解统一响应格式

---

## 后端与前端

| 层         | 职责                                     |
| ---------- | ---------------------------------------- |
| **前端**   | 展示界面、收集用户输入、发送 HTTP 请求   |
| **API**    | 接收请求、验证参数、调用数据库、返回响应 |
| **数据库** | 存储和检索数据                           |

**前端不能直接访问数据库**——会暴露密码和数据，也无法做权限控制

---

## API Route

### 路由概念

**路由 = URL 路径决定展示什么内容**

在 Next.js 中，文件夹结构就是 URL 结构：

```
app/
├── page.tsx                    → yourdomain.com/
├── subject/
│   └── [id]/
│       └── page.tsx            → yourdomain.com/subject/37311135
```

### API Route vs 页面路由

|      | 页面路由       | API 路由           |
| ---- | -------------- | ------------------ |
| 文件 | `page.tsx`     | `route.ts`         |
| 返回 | 用户看到的页面 | 程序用的 JSON 数据 |
| 位置 | `app/`         | `app/api/`         |

示例：

- `app/api/movies/route.ts` → `/api/movies`
- `app/api/movies/[id]/route.ts` → `/api/movies/123`

---

## RESTful 规范

### 核心思想

**用 URL 表示资源，用 HTTP 方法表示操作**

| HTTP 方法 | URL               | 操作               |
| --------- | ----------------- | ------------------ |
| GET       | `/api/movies`     | 获取电影列表       |
| POST      | `/api/movies`     | 创建新电影         |
| DELETE    | `/api/movies/123` | 删除 id=123 的电影 |

---

## 参数的三种来源

| 参数类型      | 示例                | 作用                     |
| ------------- | ------------------- | ------------------------ |
| **路径参数**  | `/subject/37311135` | 标识"哪个资源"           |
| **查询参数**  | `?status=P`         | 控制"怎么查"             |
| **请求 Body** | 表单提交的数据      | 传递"要创建或修改的数据" |

---

## 统一响应格式

**约定**：

|      | 格式                                            |
| ---- | ----------------------------------------------- |
| 成功 | `{ success: true, data: ... }`                  |
| 失败 | `{ success: false, error: { message: "..." } }` |

前端处理逻辑：先看 `success`，为 true 取 `data`，为 false 显示错误信息

---

## 你的工作

### 不需要手写每个接口

你只需要告诉 AI 需求，它会生成完整代码

### 你的工作是验证结果

1. **跑起来了吗？** —— `pnpm dev` 启动，页面能正常操作
2. **数据库对吗？** —— 打开 Drizzle Studio，看数据是否正确写入
3. **报错了吗？** —— 把终端里的报错整段复制给 AI，让它修

---

## 本章验收清单

- [ ] 理解后端与前端职责划分
- [ ] 掌握 API Route 创建方式
- [ ] 理解 RESTful 规范
- [ ] 掌握参数三种来源
- [ ] 理解统一响应格式

---

**下一步：第 8 章 安全与用户认证 →**
